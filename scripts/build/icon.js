/**
 * Class to represent an icon.
 */
class Icon {
  constructor(name, paths, keywords = []) {
    this.name = name;
    this.paths = paths;
    this.keywords = keywords;
  }
}

export default Icon;
